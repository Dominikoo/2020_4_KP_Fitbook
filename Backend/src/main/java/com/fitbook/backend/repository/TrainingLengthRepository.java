package com.fitbook.backend.repository;

import com.fitbook.backend.model.TrainingLength;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrainingLengthRepository extends JpaRepository<TrainingLength, Long> {
}
